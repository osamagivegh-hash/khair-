# Database - MongoDB Atlas
# For local development and production:
DATABASE_URL="mongodb+srv://osamashaer66_db_user:990099@mawaddah.lh79hv8.mongodb.net/?appName=Mawaddah"

# Note: Make sure to add your Cloud Run IP to MongoDB Atlas Network Access whitelist
# Or use 0.0.0.0/0 to allow all IPs (less secure, but easier for Cloud Run)

# Environment
NODE_ENV="development"

# Server Port (Cloud Run sets this automatically)
# PORT=8080

# Google Cloud Secret Manager (for sensitive keys)
# Access secrets via: gcloud secrets versions access latest --secret="SECRET_NAME"
# Example: API_KEY=$(gcloud secrets versions access latest --secret="api-key")
