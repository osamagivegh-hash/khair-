# Database
# For local development:
DATABASE_URL="file:./dev.db"

# For Google Cloud Run production:
# DATABASE_URL="file:/data/dev.db"
# Note: Cloud Run filesystem is ephemeral. Use Cloud Storage for persistence (see CLOUD_DEPLOYMENT.md)

# Environment
NODE_ENV="development"

# Server Port (Cloud Run sets this automatically)
# PORT=8080

# Google Cloud Secret Manager (for sensitive keys)
# Access secrets via: gcloud secrets versions access latest --secret="SECRET_NAME"
# Example: API_KEY=$(gcloud secrets versions access latest --secret="api-key")
